// import Transporter from "../config/Mailer";
// import dotenv from "dotenv";
// dotenv.config();

// const { EMAIL_USER, EMAIL_PASS } = process.env;

// const sendEmailNewPassword = async (email: string, password: string) => {
//   await Transporter.sendMail({
//     from: `"REESTABLECER CONTRASEÑA 👻" <${EMAIL_USER}>`, // sender address
//     to: `${email}`, // list of receivers
//     subject: "Nueva Cuenta ✔", // Subject line
//     // text: "Hello world?", // plain text body
//     html: `<b>Con el siguiente código podra reestablecer su contraseña</b>
//         <b> CÓDIGO:  ${password}</b>
//         `, // html body
//   });
// };

// export default sendEmailNewPassword;
